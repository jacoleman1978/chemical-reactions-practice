import { EquationCompound } from "../../configurations/interfaces";
import { Compound } from "../../../compounds/configurations/interfaces";

export const makeTargetEquationCompound = (cationName: string, anionName: string) => {
    
};
