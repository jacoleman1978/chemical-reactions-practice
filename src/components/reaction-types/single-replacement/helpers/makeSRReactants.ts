import { getSRElements } from "./getSRElements";


export const makeSRReactants = (isMetal: boolean, isSuccessful: boolean) => {
    const {element, compoundElement} = getSRElements(isMetal, isSuccessful);



};
